#include <catch2/catch.hpp>
#include <deserialize_json.h>
// #include <imgui.h>
#include <vector>

TEST_CASE("Test Case", "[ScrollingBuffer]") {
    /*ScrollingBuffer scrollingBuffer(2);
    scrollingBuffer.addPoint(1, 2);
    scrollingBuffer.addPoint(3, 4);
    scrollingBuffer.addPoint(5, 6);
    // ImVector<DataPoint> testData{ DataPoint(3, 4), DataPoint(5, 6) };

    REQUIRE(scrollingBuffer.maxSize == 2);
    // REQUIRE(scrollingBuffer.data == testData); */
    REQUIRE(0 == 0);
}
